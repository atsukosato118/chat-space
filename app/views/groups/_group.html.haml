.side__main__content
  = link_to group_messages_path(group) do
    %p.side__main__content__name
      = group.name
    %p.side__main__content__message
      - if group.messages.last.try(:body).present?
        = group.messages.last.body
      - elsif group.messages.last.try(:image).present?
        画像が投稿されています
      - else
        メッセージがありません
